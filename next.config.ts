import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins:["goggles-elitism-procreate.ngrok-free.dev"]
};

export default nextConfig;
